type: object
properties:
  context:
    allOf:
      - $ref: "../../../../schema/Context.yaml"
      - properties:
          action:
            enum:
              - get_cancellation_reasons
        required:
          - action
required:
  - context
