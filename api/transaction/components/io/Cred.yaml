type: object
properties:
  context:
    allOf:
      - $ref: "../../../../schema/Context.yaml"
      - properties:
          action:
            enum:
              - cred
        required:
          - action
  message:
    $ref: "../../../../schema/CredentialRequest.yaml"
required:
  - context
  - message
