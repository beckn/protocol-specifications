type: object
properties:
  context:
    allOf:
      - $ref: "../../../../schema/Context.yaml"
      - properties:
          action:
            enum:
              - cred
        required:
          - action
  message:
    type: object
    properties:
      creds:
        type: array
        items:
          $ref: "../../../../schema/Credential.yaml"
    required:
      - creds
required:
  - context
  - message
