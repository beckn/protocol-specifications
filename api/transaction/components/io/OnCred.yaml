type: object
properties:
  context:
    allOf:
      - $ref: "../../../../schema/Context.yaml"
      - properties:
          action:
            enum:
              - on_cred
        required:
          - action
  message:
    $ref: "../../../../schema/CredentialResponse.yaml"
  error:
    $ref: "../../../../schema/Error.yaml"
required:
  - context
