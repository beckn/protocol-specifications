description: A label under which a collection of items can be grouped.
type: object
properties:
  id:
    description: ID of the category
    type: string
  parent_category_id:
    $ref: "#/properties/id"
  descriptor:
    $ref: "./Descriptor.yaml"
  time:
    $ref: "./Time.yaml"
  ttl:
    $ref: "./Duration.yaml"
  tags:
    type: array
    items:
      $ref: "./TagGroup.yaml"

