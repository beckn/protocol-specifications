{
    "description": "Describes the tracking info of an object",
    "type": "object",
    "properties" : { 
        "url" : {
            "type" : "string",
            "format" : "uri"
        },
        "required_params" : {
            "type" : "object",
            "description": "Required params for tracking. To be defined by domain."
        }
    }
}