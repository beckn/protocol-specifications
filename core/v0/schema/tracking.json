{
    "description": "Describes the tracking info of an object",
    "type": "object",
    "properties" : { 
        "tl_method" : {
            "type" : "string",
            "enum" : ["http/get", "ws"]
        },
        "url" : {
            "type" : "string",
            "format" : "uri"
        },
        "status" : {
            "type" : "string",
            "enum" : ["active", "inactive"]
        }
    }
}