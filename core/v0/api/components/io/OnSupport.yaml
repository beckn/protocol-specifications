type: object
properties:
  context:
    allOf:
      - $ref: "../schemas/Context.yaml"
      - properties:
          action:
            enum:
              - on_support
        required:
          - action
  message:
    type: object
    properties:
      support:
        $ref: "../schemas/Support.yaml"
  error:
    $ref: "../schemas/Error.yaml"
required:
  - context
